# CRISPR Guide RNA Optimizer — OpenEnv
# Install: pip install -r requirements.txt

# Web server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# OpenEnv typed models
pydantic>=2.7.0

# NCBI fetcher
requests>=2.31.0

# Inference script
openai>=1.30.0

# NOTE: The scoring engine uses pure Python math — numpy/scipy are NOT required.
# Heavy optional deps (notebooks/analysis only) are in requirements-dev.txt